# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(viso2TaskLib)
ADD_LIBRARY(${VISO2_TASKLIB_NAME} SHARED 
    ${VISO2_TASKLIB_SOURCES})
add_dependencies(${VISO2_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${VISO2_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${VISO2_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${VISO2_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${VISO2_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${VISO2_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${VISO2_TASKLIB_HEADERS}
    DESTINATION include/orocos/viso2)

